#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../commitizen/bin/git-cz" "$@"
  ret=$?
else 
  node  "$basedir/../commitizen/bin/git-cz" "$@"
  ret=$?
fi
exit $ret
